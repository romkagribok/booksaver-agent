# Claude Code guidance

Read and follow [`AGENTS.md`](AGENTS.md); it is the shared, authoritative repository guidance.
Do not duplicate project status, architecture, or story counts here.

Claude-specific discovery files live in `.claude/`. The specsmd slash commands route to the
canonical framework under `.specsmd/aidlc/`, while `.claude/agents/sonnet-worker.md` defines the
bounded implementation worker used for parallel execution tasks.
